package BaiTap;

public class Lesson3 {
    // excuteUpdate trả về số dòng bị ảnh hưởng bới câu lệnh SQL
    // Nếu câu lệnh trả về số dòng ảnh hưởng >0 thì có dữ liệu thay đổi và ngược lại
    // Vì vậy nên có thể dùng nếu dữ lieu trả về 0 thì mã giường k tồn tại
//    String sql = "UPDATE Beds SET bed_status = 'Occupied' WHERE bed_id = " + inputId;
//
//    int rows = stmt.executeUpdate(sql);
//
//if (rows > 0) {
//        System.out.println("Đã cập nhật giường bệnh thành công!");
//    } else {
//        System.out.println("Mã giường không tồn tại!");
//    }
//}
}