package BaiTap;

public class Lesson2 {
    // Phần 1
    // Vì lệnh rs.next chỉ kiểm tra và xử lý một lần với dòng đầu tiên nên không thể in ra cả danh sách
    // Phần 2:
//    ResultSet rs = stmt.executeQuery("SELECT medicine_name, stock FROM Pharmacy");
//
//while (rs.next()) {
//        System.out.println(
//                "Thuốc: " + rs.getString("medicine_name") + " Số lượng: " + rs.getInt("stock"));
//    }
//}
}